import React from 'react';
import ReactDOM from 'react-dom';

class MyForm extends React.Component {
    render() {
        return (
            <b>Label : </b>
        );
    }
}
ReactDOM.render(<MyForm />, document.getElementById('Formlabel'));
